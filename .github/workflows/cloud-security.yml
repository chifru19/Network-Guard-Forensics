name: Cloud Security Suite CI
on:
  push:
    branches: [ main ]

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Simple Check
        run: echo "GitHub Actions is finally awake!"